# 2026-08-31 공개 데모·배포 상태 점검 기록

심사 코멘트 반영 체크포인트 직후 관리형 배포 생성이 프로젝트 서비스 할당량 제한으로 실패했다는 상태가 보고되었습니다. 이 상태는 애플리케이션 코드의 TypeScript·테스트·프로덕션 빌드 실패와는 별도로 분리해 확인합니다.

같은 시점의 개발 서버·브라우저 콘솔·네트워크 기록에서는 새 예외와 4xx·5xx 응답을 확인하지 못했습니다. 공개 `/demo?lang=ko`는 첫 접속에서 공통 대시보드 준비 화면을 잠시 보인 뒤, 후속 확인에서 기존 읽기 전용 심사위원 데모를 정상 렌더링했습니다. 이 확인 시점의 공개 화면에는 최신 인터뷰 기반 개선 미리보기 카드가 아직 보이지 않아, 배포 반영 또는 캐시 상태를 별도로 다시 확인해야 합니다.

이 문서는 오류 원인을 숨기지 않고 기록하기 위한 운영 메모이며, 실제 사용자 수·현장 성과·실제 설비 연동을 의미하지 않습니다.

후속 캐시 우회 공개 접속(`release=4d8d886f`)에서는 첫 화면에 공통 준비 화면이 잠시 표시된 뒤, 읽기 전용 심사위원 데모와 “다음 주 반영 예정 · 인터뷰 기반 개선 미리보기” 패널이 정상적으로 렌더링되었습니다. 따라서 이번 관찰에서 새 코드의 지속적인 렌더링 오류는 재현되지 않았습니다. 초기 공백·준비 화면은 배포 인스턴스 기동 또는 정적 자산 수신 직후의 일시 상태로 기록하며, 지속적으로 유지될 경우에는 새로고침 후에도 10초 이상 지속되는 화면 캡처와 접속 주소를 기준으로 다시 분리 진단합니다.
