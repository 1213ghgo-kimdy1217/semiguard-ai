# SemiGuard AI 모바일 소셜 로그인 검증 체크리스트

> [README로 돌아가기](README.md) · [작업 기록](todo.md) · [소셜 로그인 콜백 테스트](server/socialOAuth.callback.test.ts)

이 문서는 **실제 Google·Naver·Kakao 계정과 모바일 브라우저가 있어야만 수행할 수 있는 검증**을 정리합니다. 개발 미리보기는 등록된 OAuth 콜백 주소와 달라 소셜 로그인이 의도적으로 비활성화되므로, 반드시 배포 사이트 `https://semiguardai-jifnzsvd.manus.space/login`에서 진행합니다.

> 소셜 로그인은 신규 계정을 만드는 경로가 아닙니다. 먼저 회사 명찰 번호와 비밀번호로 SemiGuard AI 계정을 만든 뒤, 대시보드 메뉴에서 해당 소셜 계정을 연결한 경우에만 로그인에 사용합니다.

## 1. 공통 준비와 개인정보 보호

검증자는 본인 또는 팀이 관리하는 테스트용 소셜 계정만 사용합니다. 실제 비밀번호, OAuth 앱의 클라이언트 시크릿, 인증 코드, 액세스 토큰, 쿠키 값은 채팅·문서·화면 녹화에 남기지 않습니다. 계정 연결이 끝난 화면만 기록하고, 이름·이메일·프로필 사진 등 개인 식별 정보가 보이면 가린 뒤 공유합니다.

| 순서 | 할 일 | 성공 기준 |
|---|---|---|
| 1 | 모바일 Chrome 또는 Safari에서 배포 사이트를 엽니다. | 주소가 `https://semiguardai-jifnzsvd.manus.space`로 시작하고, 개발 미리보기가 아닙니다. |
| 2 | 명찰 번호·비밀번호로 기존 테스트 계정에 먼저 로그인합니다. | 보호된 대시보드가 열리고 새로고침 뒤에도 로그인 상태가 유지됩니다. |
| 3 | 대시보드 메뉴에서 하나의 소셜 계정을 연결합니다. | 연결 완료 안내가 보이며, 연결 전의 기본 계정이 바뀌지 않습니다. |
| 4 | 로그아웃한 뒤 브라우저 탭을 닫고 로그인 페이지를 다시 엽니다. | 이전 화면의 임시 상태에 의존하지 않는 새 로그인 흐름을 시작합니다. |

## 2. 제공자별 실계정 검증

각 제공자마다 같은 절차를 **한 번씩** 수행합니다. 한 제공자에서 실패했다고 다른 제공자의 연결을 삭제하거나 새 계정을 반복 생성하지 않습니다.

| 제공자 | 조작 | 화면에서 확인할 성공 기준 | 기록할 최소 정보 |
|---|---|---|---|
| Google | `Google로 로그인`을 누르고 승인된 연결 계정을 선택합니다. | 제공자 인증 뒤 `/` 대시보드로 돌아오고, 새로고침 후에도 로그인 화면으로 돌아가지 않습니다. | 날짜, 모바일 OS·브라우저, 성공/실패, 대시보드 진입 여부 |
| Naver | `네이버로 로그인`을 누르고 이미 연결한 계정으로 승인합니다. | 네이버 인증 뒤 같은 기본 SemiGuard 계정의 대시보드가 열립니다. | 날짜, 모바일 OS·브라우저, 성공/실패, 대시보드 진입 여부 |
| Kakao | `카카오로 로그인`을 누르고 이미 연결한 계정으로 승인합니다. | 카카오 인증 뒤 대시보드가 열리고 새로고침 후 세션이 유지됩니다. | 날짜, 모바일 OS·브라우저, 성공/실패, 대시보드 진입 여부 |

로그인 성공 여부는 **대시보드 진입 → 새로고침 후 유지 → 로그아웃 후 로그인 화면 복귀**의 세 단계로 판단합니다. 기술 담당자가 개발자 도구를 사용할 수 있는 경우에만 추가로 `auth.me` 응답에 기존 SemiGuard 계정의 `openId`와 유효한 `loginMethod`가 있는지 확인합니다. 이때도 응답 전문·쿠키·토큰은 저장하거나 공유하지 않습니다.

## 3. 의도된 실패 경로 확인

연결하지 않은 소셜 계정으로 로그인하면 계정이 자동 생성되지 않아야 합니다. 로그인 화면에는 “먼저 명찰 번호와 비밀번호로 회원가입·로그인한 뒤 대시보드 메뉴에서 연결”하라는 안내가 표시되어야 하며, 대시보드 세션이 생성되면 안 됩니다. 이미 다른 SemiGuard 계정에 연결된 제공자 계정은 연결 중복 안내를 표시해야 합니다.

Kakao에서 실패 안내가 보이면 카카오 로그인 활성화, 배포 도메인의 리다이렉트 URI, 프로필 닉네임 동의 항목을 **Kakao Developers에서 팀이 직접** 확인합니다. Google·Naver도 제공자 콘솔의 배포 콜백 주소가 배포 사이트와 일치하는지 확인하되, 앱 키·시크릿을 화면에 노출하지 않습니다.

## 4. 결과 기록과 보류 기준

| 제공자 | 기기·브라우저 | 연결 완료 | 로그인·새로고침·로그아웃 | 오류 안내 | 최종 상태 |
|---|---|---:|---:|---|---|
| Google | 예: Android Chrome | 예/아니오 | 통과/실패 | 필요한 경우 요약만 기록 | 검증 대기/통과/설정 확인 |
| Naver | 예: iPhone Safari | 예/아니오 | 통과/실패 | 필요한 경우 요약만 기록 | 검증 대기/통과/설정 확인 |
| Kakao | 예: Android Chrome | 예/아니오 | 통과/실패 | 필요한 경우 요약만 기록 | 검증 대기/통과/설정 확인 |

이 문서만으로 실계정 검증이 완료되는 것은 아닙니다. 실제 모바일 인증을 한 뒤 위 표의 **최종 상태**를 채우고, 실패한 경우에는 제공자·브라우저·보이는 오류 문구만 남긴 뒤 재현 조건을 검토합니다. 개인 계정 정보나 비밀값을 공유하기 전에는 반드시 삭제합니다.
