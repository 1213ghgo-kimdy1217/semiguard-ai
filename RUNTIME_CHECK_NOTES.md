# 런타임 점검 메모

## 2026-08-21 공개 심사위원 데모 응답 점검

`/demo?lang=en` 공개 경로는 HTTP 점검에서 본문 수신 후 연결 종료가 지연되는 양상이 있었으며, 브라우저 점검 두 차례에서 `SemiGuard AI` 문서 제목까지 확인되었지만 화면 요소를 추출하지 못했습니다. 운영 로그에는 서버 시작과 세션 쿠키 부재 안내만 있었고, 예외·스택 추적은 확인되지 않았습니다. 다만 별도 텍스트 추출 경로에서는 영어 읽기 전용 데모의 브랜드·읽기 전용 경계·데모 모드·탐지 방식 한계·3단계·위험 점수·4개 센서 근거가 모두 정상 추출됐습니다. 따라서 현재 증상은 공개 페이지 자체의 빈 화면이 아니라 해당 브라우저 점검 세션의 화면 수집 제약으로 분리합니다.

다른 공개 경로 중 `/`·`/demo?lang=ko`는 HTTP 200 상태 코드가 수신됐고, `/demo?lang=ja`는 HTTP 200 응답을 정상 완료했습니다.
