# 런타임 점검 메모

## 2026-08-21 공개 심사위원 데모 응답 점검

`/demo?lang=en` 공개 경로는 HTTP 점검에서 본문 수신 후 연결 종료가 지연되는 양상이 있었으며, 브라우저 점검 두 차례에서 `SemiGuard AI` 문서 제목까지 확인되었지만 화면 요소를 추출하지 못했습니다. 운영 로그에는 서버 시작과 세션 쿠키 부재 안내만 있었고, 예외·스택 추적은 확인되지 않았습니다. 다만 별도 텍스트 추출 경로에서는 영어 읽기 전용 데모의 브랜드·읽기 전용 경계·데모 모드·탐지 방식 한계·3단계·위험 점수·4개 센서 근거가 모두 정상 추출됐습니다. 따라서 현재 증상은 공개 페이지 자체의 빈 화면이 아니라 해당 브라우저 점검 세션의 화면 수집 제약으로 분리합니다.

다른 공개 경로 중 `/`·`/demo?lang=ko`는 HTTP 200 상태 코드가 수신됐고, `/demo?lang=ja`는 HTTP 200 응답을 정상 완료했습니다.

## 2026-08-21 공개 운영 경로 재확인

최신 운영 버전에서 `/`, `/demo?lang=ko`, `/demo?lang=en`, `/demo?lang=ja`에 대해 각각 30초 제한 HTTP 요청을 수행했고, 네 경로 모두 HTTP 200으로 정상 응답했습니다. 이 점검은 공개 경로의 서버 응답 상태만 검증하며, 실제 소셜 인증과 인쇄 대화상자 흐름은 사용자 브라우저·외부 인증이 필요한 보류 검증으로 유지합니다.

## 2026-08-21 개발 미리보기 화면 점검

로그인된 개발 미리보기의 일본어 대시보드 상단 화면에서 헤더·데모 배너·대시보드/이상 이력 탭·기간 분석 선택·CSV/PDF/공유/메일 제어·제품 이용 지표·예상 절감 비용·AI 상담 진입 제어가 화면 폭 안에 표시되는 것을 확인했습니다. 기간 분석과 지표는 데이터 요청 중 스켈레톤 상태로 표시됐으며, 해당 화면 관찰에서는 요소 겹침이나 잘림이 보이지 않았습니다.

## 2026-08-21 기간 분석·제품 지표 상태 점검

기간 분석 통계 스켈레톤과 제품 사용 지표 스켈레톤은 각각 `role="status"`, `aria-live="polite"`, `aria-atomic="true"`, 한국어·영어·일본어 상태 이름을 이미 제공함을 확인했습니다. 최신 네트워크 로그에는 4xx/5xx 항목이 없었습니다. 브라우저 콘솔에서 발견된 `Failed to fetch` 변이 오류는 2026-08-19 기록으로 현재 점검 이전의 과거 항목이며, 이번 개발 미리보기에서는 기간 분석·제품 지표가 로딩 상태로 표시되고 서버 응답 실패는 재현되지 않았습니다.

## 2026-08-21 모바일 로그인 전환 화면 점검

375×812 미리보기에서 일본어 로그인 상태 확인 로딩 화면이 화면 폭에 맞춰 표시됐습니다. 브랜드·상태 제목·설명·스피너가 중앙 정렬로 유지됐고, 가로 넘침이나 요소 잘림은 관찰되지 않았습니다. 이 캡처는 인증 대기 중의 전환 상태만 검증한 것이며, 실제 소셜 인증 완료 흐름은 외부 인증이 필요한 보류 검증으로 유지합니다.

## 2026-08-21 공개 심사위원 데모 모바일 화면 점검

375×812 미리보기에서 한국어·영어·일본어 공개 심사위원 데모의 브랜드 카드·언어 선택·데모 모드 배너·시뮬레이션 데이터 안내·탐지 방식 설명·3단계 탭이 화면 폭 안에 표시되는 것을 확인했습니다. 각 언어에서 제목과 안내 문구가 해당 언어로 렌더링됐으며, 카드·탭·닫기 제어에서 가로 넘침이나 겹침은 관찰되지 않았습니다.

## 2026-08-21 로그인 모바일 화면 점검

375×812 미리보기의 일본어 로그인 화면에서 언어 전환·브랜드·명찰번호·비밀번호·표시 전환·로그인·회원가입·심사위원 데모·소셜 연결 안내와 Google/Naver/Kakao 연결 제어가 한 열 레이아웃으로 표시됐습니다. 제어 간 간격과 텍스트 줄바꿈이 유지됐고, 화면 관찰에서는 가로 넘침·중첩·잘림이 보이지 않았습니다.

## 2026-08-21 회원가입 모바일 화면 점검

375×812 미리보기의 일본어 회원가입 화면에서 언어 전환·브랜드·명찰번호·이름·생년월일·날짜 표기 안내·비밀번호 강도·비밀번호 표시 전환·계정 등록·로그인 이동 제어가 한 열로 표시됐습니다. 입력과 보조 제어의 간격·줄바꿈이 유지됐고, 화면 관찰에서는 가로 넘침·중첩·잘림이 보이지 않았습니다.

## 2026-08-21 전체 회귀·프로덕션 빌드 재검증

전체 Vitest 회귀는 253개 파일·589개 테스트를 모두 통과했습니다. `pnpm build`는 Vite 프런트엔드와 서버 번들을 성공적으로 생성했으며, Dashboard 번들의 500KB 초과 경고는 기존 수동 청크 분리 금지 계약에 따른 알려진 경고로 유지했습니다. 작업트리는 검증 후 변경 없이 깨끗한 상태였습니다.

## 2026-08-21 최신 오류 타임스탬프 점검

브라우저 콘솔에 남은 최근 오류는 2026-08-19의 회원가입 실패 시도와 미리보기 환경의 `Failed to fetch` 기록이었습니다. 최신 네트워크 로그에는 4xx·5xx 응답이 없었으므로, 현재 실행 중인 개발 서버의 기능 장애로 분류하지 않았습니다. 실제 회원가입·소셜 인증의 재현 검증은 계정·외부 인증이 필요한 보류 항목으로 유지합니다.

## 2026-08-21 로그인된 대시보드 브라우저 흐름 재확인

개발 미리보기에서 초기 인증 확인 화면 뒤 로그인된 대시보드가 정상 렌더링되는 것을 확인했습니다. 위험 경보 대화상자, AI 분석 대체 경로 안내, 운영 상태, 기간별 분석 제어, 센서 카드·차트, 가상 설비 상태 주입 제어, 월간 히트맵, 첫 분석 가이드 진입 제어가 브라우저 접근성 트리에 노출됐습니다. 센서·위험 차트의 키보드 포인트에는 점수·단계·시각을 포함한 안내가 제공됐고, 이 점검에서는 새 콘솔 오류나 네트워크 4xx·5xx를 확인하지 못했습니다. 실제 소셜 인증 완료는 외부 인증이 필요한 보류 검증으로 유지합니다.

## 2026-08-21 대시보드 데이터 로딩 완료 재확인

로그인된 개발 미리보기에서 초기 스켈레톤 뒤 운영 상태·제품 사용 지표·예상 절감 비용·4개 센서 현재값과 추이·위험 점수·가상 설비 상태 주입·월간 히트맵이 정상 데이터 상태로 전환됐습니다. 기간 분석의 CSV 제어는 초기 데이터 요청 중 로딩 안내를 유지했고, 차트의 개별 키보드 포인트에는 순번·점수·단계·시각과 좌우 이동 방법이 제공됐습니다. 화면 관찰에서는 데이터 전환 뒤 가로 넘침·제어 중첩·빈 화면을 확인하지 못했습니다.

## 2026-08-21 기간 분석 PDF 로딩 안내 브라우저 확인

최신 개발 미리보기에서 기간 분석 초기 로딩 중 CSV와 PDF 보고서 제어가 모두 “기간 분석 데이터를 불러오는 중”이라는 접근성 이름으로 노출되는 것을 확인했습니다. 데이터 로딩 완료 뒤에는 각각 CSV 내보내기와 PDF 보고서 내보내기 이름으로 정상 복귀했고, 운영 지표·4개 센서 값·위험 점수·차트도 함께 정상 데이터 상태로 전환됐습니다.

## 2026-08-21 최신 운영·개발 오류 점검

최신 운영 도메인의 `/`, `/demo?lang=ko`, `/demo?lang=en`, `/demo?lang=ja`는 모두 HTTP 200을 반환했습니다. 또한 개발 미리보기의 최근 브라우저 콘솔 180행과 네트워크 요청 240행을 점검했으며, 새로운 콘솔 오류와 네트워크 4xx·5xx 응답은 확인되지 않았습니다. Dashboard 대형 청크 경고는 알려진 번들 경고로 유지합니다.

## 2026-08-21 운영 경로 재점검

최신 운영 버전 저장 뒤 대시보드와 한국어·영어·일본어 심사위원 데모 네 공개 경로를 다시 요청했고, 모두 HTTP 200을 반환했습니다.

## 2026-08-21 대시보드 초기 로딩·첫 사용 피드백 시각 점검

1280px 개발 미리보기에서 초기 로딩 스켈레톤이 기간 분석·제품 사용 지표·통계 카드 영역에 정렬된 상태로 표시되는 것을 확인했습니다. 데이터 전환 뒤에는 첫 사용 피드백 대화상자가 배경을 흐리게 처리한 채 중앙에 표시됐고, 5점 선택·단계 선택·나중에·선택 응답 저장 제어의 겹침이나 가로 잘림은 확인되지 않았습니다.

## 2026-08-21 모바일 대시보드·첫 사용 피드백 시각 점검

375px 개발 미리보기에서 초기 기간 분석 제어와 스켈레톤 카드가 단일 열·두 열 흐름으로 가로 넘침 없이 표시됐습니다. 데이터 전환 뒤 첫 사용 피드백 대화상자도 모바일 너비 안에서 5점 선택·단계 선택·나중에·선택 응답 저장 제어가 겹치지 않고 표시됐습니다.

## 2026-08-21 심사위원 데모 탐지 방식 안내 모바일 점검

375×812 미리보기에서 한국어·영어·일본어 공개 심사위원 데모의 브랜드 카드·언어 선택·데모 모드 배너·시뮬레이션 데이터 경계·확장 가능한 탐지 방식과 검증 상태 패널·위험 신호 단계가 화면 폭 안에 표시됐습니다. 각 언어의 탐지 방식 요약은 학습된 고장 분류 모델이 아닌 규칙형 계산이라는 현재 구현 경계를 유지했으며, 패널·언어 선택·단계 제어에서 가로 넘침이나 중첩은 관찰되지 않았습니다. 세부 입력·학습 데이터 부재·구현 검증·현장 정확도 미검증 문구는 해당 패널을 펼쳐 확인하는 구조로 제공됩니다.

## 2026-08-21 심사 피드백 보완 후 공개 경로 점검

탐지 엔진 설명·개발 과정·실증 증거 자료를 보강한 최신 운영 버전에서 `/`, `/demo?lang=ko`, `/demo?lang=en`, `/demo?lang=ja`를 각각 30초 제한으로 요청했습니다. 네 공개 경로 모두 HTTP 200으로 응답했습니다. 이 점검은 공개 경로의 서버 응답 상태를 확인한 것이며, 실제 팹 정확도·사용자 성과·소셜 인증 완료 흐름은 별도의 외부 증거 또는 인증이 필요한 보류 상태로 유지합니다.

## 2026-08-21 심사위원 데모 탐지 방식 안내 데스크톱 점검

1280×720 전체 화면에서 한국어·영어·일본어 공개 심사위원 데모를 확인했습니다. 세 언어 모두 헤더·데모 모드 경계·시뮬레이션 데이터 고지·`탐지 방식과 검증 상태` 아코디언·위험 신호 카드·단계 제어가 좌우 넘침 없이 표시됐습니다. 아코디언의 기본 요약은 현재 구현이 학습된 고장 분류 모델이 아닌 규칙형 계산임을 각 언어로 제시하며, 세부 입력·학습 데이터 부재·구현 검증·현장 정확도 미검증은 확장 패널에 유지됩니다.

## 2026-08-21 GitHub 동기화 후 한국어 공개 데모 재점검

GitHub `main` 동기화가 확인된 최신 운영 버전에서 한국어 공개 심사위원 데모를 375×812와 1280×720으로 다시 확인했습니다. 두 화면 폭 모두 브랜드 카드·언어 선택·데모 모드 배너·시뮬레이션 데이터 경계·탐지 방식과 검증 상태 안내·위험 신호 단계·점수·4개 센서 카드·이전/데모 시작 제어가 가로 넘침이나 겹침 없이 표시됐습니다. 같은 점검 주기에서 `/`, `/demo?lang=ko`, `/demo?lang=en`, `/demo?lang=ja`는 모두 HTTP 200을 반환했고, 최근 개발 미리보기 콘솔과 네트워크 로그에서도 신규 오류 또는 4xx·5xx 응답을 확인하지 못했습니다.

## 2026-08-21 공개 운영 도메인 일시적 지연·복구 확인

최신 운영 버전 저장 후 공개 도메인에 대한 20초 제한 HTTP 요청 네 건은 모두 시간 초과를 반환했고, 첫 브라우저 접속도 30초 뒤 `ERR_TIMED_OUT`로 나타났습니다. 프로덕션 로그에서는 같은 시각 서버 기동과 세션 쿠키 부재 안내만 확인됐으며 예외·스택 추적은 없었습니다. 이후 같은 브라우저 세션을 다시 확인한 결과 공개 대시보드가 정상 렌더링되어 로그인 전 영어 화면의 운영 상태·기간 분석·센서 데이터·위험도·시뮬레이터·첫 사용 피드백 대화상자가 모두 접근성 트리에 노출됐습니다. 따라서 이 점검에서는 지속적인 애플리케이션 장애가 아니라 배포 인스턴스 초기 응답 지연으로 분리하며, 재발 시 운영 로그와 실제 사용자 접속 시간을 함께 기록합니다.

## 2026-08-21 공개 심사위원 데모 CLI 연결 오류 분리 점검

CLI의 HTTPS 요청은 한국어·영어·일본어 심사위원 데모에서 `SSL_ERROR_SYSCALL`을 반환했지만, 브라우저로 한국어와 영어 공개 데모를 직접 확인한 결과 모두 정상 렌더링됐습니다. 두 언어에서 읽기 전용 경계·데모 모드·시뮬레이션 데이터 고지·규칙형 탐지 방식과 검증 한계·3단계 탭·위험 점수·4개 센서 근거·정식 대시보드 로그인 제어가 정상 노출됐습니다. 따라서 이번 CLI SSL 오류는 공개 심사위원 데모 화면 장애로 분류하지 않으며, 실제 사용자 브라우저 재현 여부와 함께 추적합니다.

## 2026-08-21 공개 운영 도메인 반복 시간 초과·개발 서버 분리 점검

이후 프로덕션 로그 CLI는 `cloudrun service not found`를 반환했고, 영어 공개 심사위원 데모 브라우저 접속도 다시 `ERR_TIMED_OUT`로 실패했습니다. 반면 최신 개발 서버는 재시작 후 로컬 `/` 경로에서 HTTP 200을 반환했으며, 서버 시작 로그에는 OAuth 초기화와 포트 3000 기동만 기록됐습니다. 즉 현재 코드·로컬 서버의 재현 오류는 확인되지 않았고, 공개 운영 인프라 또는 프록시 경로의 일시적 가용성 문제로 분리합니다. 외부 배포 상태는 후속 운영 저장 후 다시 확인합니다.

## 2026-08-21 공개 헬스 상태 경로 운영 연결 재확인

새 `/api/health` 엔드포인트는 로컬에서 HTTP 200, `Cache-Control: no-store`, `{"status":"ok"}`로 검증됐습니다. 최신 운영 저장 후 프로덕션 로그에는 서버 기동이 다시 기록됐지만, 공개 `https://semiguardai-jifnzsvd.manus.space/api/health`는 브라우저에서 `ERR_TIMED_OUT`, CLI에서 `SSL_ERROR_SYSCALL`을 계속 반환했습니다. 따라서 경량 애플리케이션 경로까지 외부에서 도달하지 못하는 상태이며, 앱 코드가 아닌 도메인 라우팅·TLS 프록시 또는 운영 인프라 가용성 문제라는 근거를 강화합니다.

## 2026-08-21 개발 대시보드 재시작 후 화면 전환 확인

운영 저장 직후 캡처된 개발 미리보기의 초기 로딩 화면을 재점검했습니다. 서버·브라우저 콘솔·네트워크 로그에는 새 오류 또는 4xx·5xx가 없었고, 이어진 1280×720 화면에서는 첫 사용 피드백 대화상자가 배경 대시보드 위에 정상 표시됐습니다. 선택 점수·어려웠던 단계·나중에·선택 응답 저장 제어가 화면 안에 렌더링돼, 개발 대시보드는 초기 로딩 뒤 정상 상태로 전환됨을 확인했습니다.

## 2026-08-21 공개 운영 인프라 회복 확인

공개 `/api/health` 경로가 지연 대기 뒤 HTTP 200으로 회복된 뒤, 운영 대시보드 `/`와 심사위원 데모 `/demo?lang=ko`, `/demo?lang=en`, `/demo?lang=ja`를 각각 다시 요청했습니다. 네 경로 모두 HTTP 200을 반환했습니다. 앞서 관찰된 연결 시간 초과·TLS 오류는 현재 시점에서 지속 장애가 아니며, 코드 변경 없이 운영 인프라 또는 프록시의 일시적 가용성 편차가 해소된 것으로 기록합니다.

## 2026-08-21 한국어 심사위원 데모 응답 본문 지연 분리 확인
후속 CLI 점검에서 한국어 심사위원 데모는 본문 334,960바이트 수신 뒤 연결 종료가 지연돼 시간 초과 메시지를 함께 반환했지만 최종 HTTP 상태는 200이었습니다. 같은 경로를 브라우저로 확인한 결과 한국어 읽기 전용 데모가 정상 렌더링됐고, 브랜드·데모 모드·시뮬레이션 데이터 경계·탐지 방식과 검증 상태·3단계 탭·위험 점수·4개 센서·로그인 제어가 모두 노출됐습니다. 따라서 이 현상은 화면 장애가 아닌 CLI 연결 종료 지연으로 분리합니다.

## 2026-08-25 최신 버전 복원 후 시연용 로그인 흐름 확인
최신 운영 버전 `f5f3c77c` 복원 뒤 문서화된 시연용 계정 `TEST-2026-V1`로 개발 미리보기 로그인 흐름을 확인했습니다. 로그인 중 보안 상태 안내를 거친 뒤 대시보드가 정상 표시됐으며, 운영 상태·기간 분석·센서 카드·위험도·시뮬레이터·AI 상담 진입·첫 사용 경험 대화상자의 접근성 제어가 확인됐습니다. 최근 개발 로그에는 새 브라우저 콘솔 오류나 4xx·5xx 네트워크 실패가 없었습니다.
