# SemiGuard AI 실증 검증 계획

> [README로 돌아가기](README.md) · [AI 활용과 안전장치](AI_USAGE.md) · [작업 기록](todo.md)

## 1. 현재 상태와 목적

현재 SemiGuard AI의 공개 데모와 자동 폴링은 **시뮬레이션 데이터**를 사용합니다. z-score 기반 위험 점수의 코드 동작은 회귀 테스트로 확인하지만, 실제 팹의 과거 로그나 파일럿 데이터로 정확도·오탐·미탐·사전 경고 시간을 측정한 결과는 아직 없습니다. 따라서 이 문서는 현재 성과를 주장하기 위한 자료가 아니라, 승인된 읽기 전용 데이터가 제공되었을 때 동일한 기준으로 검증하기 위한 사전 계획입니다.

## 2. 최소 데이터 계약

과거 로그 검증에는 개인 식별 정보나 생산 레시피 전체가 필요하지 않습니다. 아래 최소 항목을 장비별로 시간 순서대로 제공받고, 원본은 협력 기관의 승인 범위와 보존 정책을 따릅니다.

| 구분 | 최소 항목 | 검증에서의 용도 |
|---|---|---|
| 식별·시간 | 익명화 장비 ID, UTC 시각, 표본 간격 | 장비별 추이와 시간 누수 없는 평가 구간 구성 |
| 센서 | 전류(A), 온도(°C), 진동(mm/s), 소음(dB) | z-score 위험 점수 재계산 |
| 사건 라벨 | 승인된 점검·경보·이상 시점, 라벨 출처 | 탐지와 실제 확인 시점의 비교 |
| 선택 맥락 | 계획 정지 여부, 정비 완료 시각, 센서 결측 사유 | 오탐 해석과 제외 기준 기록 |

> 데이터 계약에는 설비 제어 권한을 포함하지 않습니다. 수집은 승인된 복제본을 읽는 일방향·읽기 전용 범위에서만 검토합니다.

## 3. 사전 등록 평가 절차

평가 전에는 장비별 기준값, 위험 단계 경계, 이상 사건의 정의와 결측 처리 규칙을 담당자와 합의해 기록합니다. 이후 시간상 이른 구간은 기준 보정용, 뒤의 구간은 잠긴 평가용으로 나누며, 평가 구간을 본 뒤 위험 단계 경계나 사건 정의를 바꾸지 않습니다. 장비·라인별 결과와 전체 평균을 구분해 보고하여 한 장비의 특성이 전체 결과를 과장하지 않도록 합니다.

| 순서 | 수행 내용 | 남기는 증거 |
|---|---|---|
| 1. 범위 고정 | 대상 장비, 관측 기간, 읽기 전용 권한, 사건 정의를 확정 | 승인 기록, 데이터 사전, 제외 기준 |
| 2. 기준 보정 | 이른 기간의 정상 운전 구간에서 장비별 기준 후보를 검토 | 기준값 표, 담당자 검토 메모 |
| 3. 잠긴 평가 | 뒤 기간의 점수를 재계산하고 사건 라벨과 비교 | 시간순 점수 이력, 비교표 |
| 4. 현장 검토 | 큰 편차·오탐·미탐 사례를 담당자와 함께 해석 | 사례 카드, 조치·개선 항목 |
| 5. 파일럿 판단 | 결과와 보안·절차 적합성을 함께 검토 | 검증 보고서, 다음 단계 승인 또는 보류 결정 |

## 4. 측정 지표와 해석 원칙

| 지표 | 계산 또는 정의 | 현재 상태 |
|---|---|---|
| 정밀도 | 실제 이상으로 확인된 경보 ÷ 전체 경보 | 실제 로그 검증 전이므로 미측정 |
| 재현율 | 경보가 선행 또는 동시 발생한 실제 이상 ÷ 전체 실제 이상 | 실제 로그 검증 전이므로 미측정 |
| 오탐·미탐 | 사건 정의와 비교해 잘못 경보된 건·놓친 건 | 실제 로그 검증 전이므로 미측정 |
| 사전 경고 시간 | 첫 경보 시각과 승인된 사건 시각의 차이 | 실제 로그 검증 전이므로 미측정 |
| 담당자 해석 가능성 | 센서 근거와 권장 점검 순서를 이해·활용할 수 있었는지에 대한 구조화 피드백 | 파일럿 전이므로 미측정 |

수치 목표는 실제 장비의 위험도, 센서 품질, 기존 점검 절차를 모르고 임의로 정하지 않습니다. 승인된 데이터 계약과 담당자 합의 후 목표·허용 범위를 정하며, 불리한 결과도 같은 정의와 기간으로 함께 보고합니다.

## 5. 보고서 형식과 다음 단계

검증 보고서는 데이터 범위, 고정한 기준값, 사건 정의, 전체·장비별 지표, 대표 오탐·미탐 사례, 담당자 피드백, 보안·운영 이슈, 다음 조치 순서로 작성합니다. 결과가 충분하지 않으면 단계 경계 조정이나 화면 근거 개선을 검토하되, 설비 제어 기능을 추가하지 않고 읽기 전용 범위를 유지합니다.

실제 파일럿을 시작하려면 협력 기관의 데이터 제공 승인, IT/OT 보안 검토, 현장 안전 절차 확인, 담당자 협의가 모두 선행되어야 합니다. 이러한 승인 전에는 공개 데모의 시뮬레이션 경계와 이 계획의 **미측정 상태**를 그대로 유지합니다.
