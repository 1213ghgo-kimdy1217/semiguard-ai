# 대시보드 슬라이드 메뉴 검증

2026-08-12 기준으로 데스크톱 미리보기 스크린샷에서 대시보드가 정상 렌더링되고 헤더 우측에 `☰ 메뉴` 버튼이 표시되는 것을 확인했다. 기존 상단 조작 버튼은 헤더에서 제거되고 메뉴 패널을 통해 제공되도록 구현했다.

메뉴 패널은 좌측에서 우측으로 등장하며, 언어 전환·다크/라이트 모드·음소거·볼륨·데모 실행 및 간격·PDF 보고서·로그아웃을 포함한다. Escape 키, 닫기 버튼, 바깥 영역 클릭으로 닫히며 열림 동안 배경 스크롤을 잠근다.

개발 미리보기의 브라우저 직접 접근은 현재 temporary website unavailable 상태로 상호작용 검증이 제한되었다. TypeScript 검사와 Vitest 17개 테스트는 통과했다.

## 열린 상태 시각 검증

개발 모드 전용 `?menu=open` 미리보기로 실제 열린 상태를 캡처했다. 데스크톱(1280×720)에서는 좌측 약 360px 패널과 반투명 오버레이가 표시되고, 모바일(390×844)에서는 화면 폭을 넘지 않는 좌측 패널로 표시된다. 두 화면 모두 언어, 라이트/다크, 음소거, 볼륨 슬라이더, 데모, PDF 내보내기, 로그아웃 컨트롤이 패널 안에 정렬되며 우측 대시보드가 가려진다.

## OAuth 개발 모드 보호

개발 미리보기의 Google OAuth는 Google Cloud Console에 배포 콜백 주소만 등록되어 있어 `redirect_uri_mismatch`가 발생할 수 있다. 이를 방지하기 위해 개발 빌드에서는 Google·Naver·Kakao 버튼을 비활성화하고, 배포 사이트에서 이용하라는 안내를 표시한다. 배포 빌드에서는 `import.meta.env.DEV`가 false이므로 기존 소셜 로그인 버튼이 정상 활성화된다. 메뉴 열린 상태 캡처는 `?menu=open` 경로로 OAuth와 독립적으로 수행한다.
